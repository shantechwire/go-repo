packages string

func Reverse(s string) string {
		b:= []rune(s)
			for i := 0; i < len(be)/2; i++ {
						J := len(b)-i-1
								b[i], b[j] = b[j], b[i]
									}
										return string(b)
									}
